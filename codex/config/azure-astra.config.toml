# Explicit Azure Astra overlay using Codex's default model metadata and protocol.
# Use with: codex --profile azure-astra

model = "gpt-6-astra"
model_provider = "azure"
model_reasoning_effort = "medium"
