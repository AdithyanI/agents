# Optional Azure Astra overlay. This does not change the desktop app default.
# Use with: codex --profile azure-astra

model = "gpt-6-astra"
model_provider = "azure"
model_reasoning_effort = "medium"
