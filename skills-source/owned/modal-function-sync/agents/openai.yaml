interface:
  display_name: "WIN Modal Integration"
  short_description: "Maintain WIN's Modal runtime, client contracts, and delivery."
  default_prompt: "Use $modal-function-sync to implement or update WIN's Modal runtime and keep its registry, generated client, tests, and release integration consistent."
